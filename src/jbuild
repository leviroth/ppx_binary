(jbuild_version 1)

(library
 ((name        ppx_binary)
  (kind ppx_rewriter)
  (synopsis "Convert records to and from binary data")
  (preprocess (pps (ppx_metaquot)))
  (libraries (ppx_type_conv ocaml-migrate-parsetree))))
